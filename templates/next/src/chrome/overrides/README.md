/**
 * Optional overrides for site chrome (navbar / footer).
 *
 * When openblog.config.ts has chrome.mode = "inherit-monorepo":
 * 1. Copy your main site's Header/Footer components here, or
 * 2. Re-export them from your monorepo package.
 *
 * Then wire them in src/app/blog/layout.tsx instead of SiteHeader/SiteFooter.
 */

export {};
